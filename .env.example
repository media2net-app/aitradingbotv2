# Database (Prisma / Postgres)
# Vul hier je eigen DATABASE_URL in, bijv. via Prisma Accelerate of direct Postgres.
# Belangrijk: nooit een echte waarde committen, alleen lokaal / in Vercel env vars.
# DATABASE_URL=prisma+postgres://...

# Optional: set in Vercel Dashboard > Settings > Environment Variables
# If set, EA must send this value in header X-API-Key when posting logs
# LOG_API_KEY=your-secret-key

